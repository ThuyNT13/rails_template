<li>
  <%= link_to user.username, user %>
  <% if %>
    <%= link_to "delete", user,
      method: :delete,
      data: { confirm: "Are you sure you want to delete #{user.username}?" } %>
  <% end %>
</li>
